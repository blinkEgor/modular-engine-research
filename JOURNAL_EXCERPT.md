# Журнал разработки: Исследование модульной архитектуры BECRUM

## 📖 О журнале
Этот документ содержит выдержки из моего рабочего журнала во время исследования модульной архитектуры на C++. Он демонстрирует не только технические решения, но и сам процесс мышления, борьбу с проблемами и постепенное формирование архитектурных принципов.

---

## 🔍 Ключевые записи

### 1. **Динамическая загрузка API в CORE**
**Проблема:** Нужно интегрировать API с ядром через динамическую загрузку вместо статической линковки.

**Решение:**
- Реализовал `ApiLoader` с использованием `dlopen/dlsym`
- Добавил чтение пути к `.so` из конфигурации и CLI-аргументов
- Внедрил обработку ошибок и подробное логирование

**Вывод:** Успешно разделил систему на независимые модули, которые можно обновлять отдельно. Это подтвердило работоспособность базовой концепции.

### 2. **Создание Fake-логгера с DI**
**Проблема:** Нужна рабочая заглушка для логгера, которая будет функционировать до загрузки плагинов.

**Решение:**
- Создал `LoggerFake` с реальной логикой (запись в файл, ротация логов)
- Внедрил зависимость логгера во все компоненты через конструкторы
- Заменил прямые вызовы `std::cerr` на использование инжектированного логгера

**Вывод:** Подготовил инфраструктуру для будущего PluginManager и обеспечил отказоустойчивость системы.

### 3. **Архитектурный рефакторинг именования**
**Проблема:** Имена классов-реализаций (Fake/Stub) имели префикс `A` как у абстракций, что нарушало семантику.

**Решение:**
- Написал скрипт для рекурсивного переименования файлов и кода
- Переименовал `ALoggerFake` → `LoggerFake`, `AOutputStub` → `OutputStub` и т.д.
- Обновил все include-директивы и ссылки

**Вывод:** Чистота именования критически важна для понимания архитектуры. Абстракции (`A*`) и реализации (`*Fake`/`*Stub`) должны чётко различаться.

### 4. **Внедрение зависимостей во все компоненты**
**Проблема:** Компоненты работали изолированно, каждый писал логи в `std::cerr`.

**Решение:**
- Добавил в каждую абстракцию protected-указатель `ALogger* m_logger`
- Реализовал конструкторы для инъекции зависимостей
- Обновил все реализации для использования общего логгера

**Вывод:** Централизованное управление зависимостями — ключ к гибкости системы. Это подготовило почву для горячей замены компонентов.

### 5. **Создание Fake-парсера для конфигов**
**Проблема:** PluginManager нуждается в парсере для чтения конфигов, но настоящий парсер может быть ещё не загружен.

**Решение:**
- Создал `ParserFake` с минимальной функциональностью
- Реализовал только методы `load_from_file`, `get` и `get_all`
- Использовал подход "один парсер — один файл" для избежания конфликтов ключей

**Вывод:** Даже минимальные реализации должны решать конкретные задачи. Fake-компоненты — это не просто заглушки, а важные части инфраструктуры.

### 6. **Обсуждение архитектуры с нейросетью**
**Проблема:** Нужно решить, как правильно организовать зависимость от `LOG_LEVEL` без циклических связей.

**Диалог с DeepSeek:**
- Предложение нейросети: вынести `LOG_LEVEL` в отдельный заголовок
- Мой анализ: это действительно решит проблему циклических зависимостей
- Решение: создал `LogLevel.h` и обновил все зависимости

**Вывод:** ИИ — полезный инструмент для валидации идей, но окончательное архитектурное решение должно приниматься разработчиком.

### 7. **Первые шаги PluginManager**
**Проблема:** Нужно начать реализацию системы плагинов.

**Решение:**
- Создал каркас `PluginManager` с базовыми методами
- Интегрировал его в API, передав зависимость логгера
- Подготовил структуру конфигурационных файлов

**Вывод:** Постепенный, итеративный подход позволяет строить сложные системы. Каждый шаг должен быть проверяемым и осмысленным.

**Aнализ:**
Осознал, что текущая архитектура API (прямое владение компонентами) может быть неидеальной для PluginManager. Нужно переосмыслить распределение ответственностей.

**Вывод:** Архитектура должна эволюционировать по мере понимания проблемы.

### 9. **Тестирование полной системы**
**Проверка:** После всех изменений нужно убедиться, что система работает.

**Результат:**
- Успешная сборка всех компонентов
- Корректная работа инъекции зависимостей
- Логирование работает через единый канал

**Вывод:** Регулярное тестирование целостности системы предотвращает накопление технического долга.

---

## 🎯 Что демонстрирует этот журнал

1. **Глубокое погружение в проблему** — от низкоуровневых деталей (`dlopen`, `dlsym`) до высокоуровневых архитектурных решений.

2. **Итеративный подход** — постоянное уточнение и переосмысление архитектуры по мере роста понимания.

3. **Работа с инструментами** — CMake, динамические библиотеки, системы сборки, shell-скрипты.

4. **Критическое мышление** — анализ предложений ИИ, взвешенные архитектурные решения.

Этот журнал — не просто запись кода, а документация инженерного мышления в действии.
